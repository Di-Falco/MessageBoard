@{
  Layout = "_Layout";
}

@using System.Security.Claims

@if (User.Identity.IsAuthenticated)
{
  <p>Hello @User.Identity.Name!</p>
}
else
{
  <a href='@Url.Action("Register")'><button>Register</button></a>
  <a href='@Url.Action("Login")'><button>Log In</button></a>
}
